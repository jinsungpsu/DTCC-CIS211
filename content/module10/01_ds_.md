# Tree ADT

Delaware Technical Community College

---

# Slide 2

![tree-diagram](images/tree-diagram.png)

---

# Data Structure Tree vs. Natural Tree

Opposite of a "nature" tree - root is at the top, no "parent"

---

# Hierarchy

Parent/ancestor

Child/descendant

---

# Tree defined

- Every node can have only one parent (except root)

---

# Parts of a tree

- Nodes or vertices
- Edges

---

# Tree Terminology

Root

Leaf

Levels/Depth/Height

---

# Recursive Definition

Recursive definition - every subtree is in itself a tree

---

# More definitions

In a tree data structure, a path refers to a sequence of nodes that connects one node to another within the tree. A path is typically represented as a list of nodes, starting from one node (often the root or a parent node) and leading to a target node (usually a descendant).

<!-- footer -->
https://chatgpt.com

---

# Why trees?

- Need to organize hierarchical data
- Organize things in such a way that the longest path to a specific item is short (from the root)
- In other words, keep nodes as close to the root as possible
- In other words, the longest path in a tree is the height of the tree

---

# Implementation…

Arrays?

Linked Nodes?

---

# Arrays?

Organizing non-linear information in a linear data structure…

Possible - we’ll explore some options later

---

# Implementation of general tree...

```java
class Tree {
  Node root;
  class Node {
    ArrayList<Node> children;
  }
}
```

---

# General trees are … too general

Operations on a general tree would be fairly generic… how do we add a node?
We have to specify EXACTLY where a node needs to go.

```java
int level1child = 2;
int level2child = 3;
int level3child = 1;
root.get(level1child).get(level2child).get(level3child).add(someData);
```

---

# Variety of specialty trees

- We start with a simple one Binary Search Tree
- Many, many others
- Heaps (min and max heaps)
- AVL
- Red-Black
- etc.
- Each of these trees have more constraints, so they’re organized with more strict rules. Therefore, the operations are also simpler.

---

# Binary Trees

---

# Restrictions…

Each node can have at most 2 children (0, 1, or 2)

---

# Recursive definition

- Every subtree of a binary tree is a binary tree

---

# Why do these things exist?

- Efficiency
- Don’t have to traverse all nodes
- O(log n) traversal/search
- Depending on height of tree

---

# Tree Traversal

- The longest path for any traversal maxes out at the height of the tree
- However, what if the item you’re looking for is in a different path?
- In other words, what if you took a wrong turn?
- Then you’d need to go back (backtrack) and traverse down a different path…
- Could be very inefficient
- So… let’s restrict the binary tree even more!

---

# Binary Search Tree (BST)

Delaware Technical Community College

---

# Why?

Facilitates Binary Searching

Storage that is always “sorted”

Splits up some of the work involved in retrieval of data

- Sorts as data is inserted
- Intuitive binary search retrieval

---

# Pros and cons

- Always think about what we’re giving up and what we’re gaining…
- What is the “expensive” part of using a BST?
- Why are we willing to pay this price?
- What’s the alternative?
- What does sorting look like in general?

---

# Basics

- The left subtree of a node contains only nodes with keys lesser than the node’s key.
- The right subtree of a node contains only nodes with keys greater than the node’s key.
- The left and right subtree each must also be a binary search tree.
